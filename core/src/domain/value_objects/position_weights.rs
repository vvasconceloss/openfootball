pub struct PositionWeights {
  pub diving: u8,
  pub handling: u8,
  pub reflexes: u8,
  pub distribution: u8,

  pub decision: u8,
  pub leadership: u8,
  pub determination: u8,

  pub pace: u8,
  pub stamina: u8,
  pub jumping: u8,
  pub strength: u8,
  pub acceleration: u8,

  pub vision: u8,
  pub passing: u8,
  pub heading: u8,
  pub crossing: u8,
  pub tackling: u8,
  pub dribbling: u8,
  pub finishing: u8,
}