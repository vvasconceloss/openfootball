pub struct PositionWeights {
  pub diving: u32,
  pub handling: u32,
  pub reflexes: u32,
  pub distribution: u32,

  pub decision: u32,
  pub leadership: u32,
  pub determination: u32,

  pub pace: u32,
  pub stamina: u32,
  pub jumping: u32,
  pub strength: u32,
  pub acceleration: u32,

  pub vision: u32,
  pub passing: u32,
  pub heading: u32,
  pub crossing: u32,
  pub tackling: u32,
  pub dribbling: u32,
  pub finishing: u32,
}